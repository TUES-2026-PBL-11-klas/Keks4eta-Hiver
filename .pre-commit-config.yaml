repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: detect-private-key

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.9
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.13.0
    hooks:
      - id: mypy
        additional_dependencies: [pydantic>=2.7.0, pydantic-settings>=2.3.0]

  # TruffleHog is enforced in CI via GitHub Actions; the Go-based pre-commit hook is disabled locally because many dev machines still use Go 1.20.
#  - repo: https://github.com/trufflesecurity/trufflehog
#    rev: v3.78.0
#    hooks:
#      - id: trufflehog
#        name: TruffleHog secret scan
#        entry: trufflehog git file://. --since-commit HEAD --only-verified --fail
#        language: golang
#        pass_filenames: false
