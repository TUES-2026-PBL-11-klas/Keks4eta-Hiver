apiVersion: v2
name: hiver
description: Hiver — FastAPI task-marketplace backend (Clean Architecture)
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Application version — the app this chart deploys.
appVersion: "0.1.0"
